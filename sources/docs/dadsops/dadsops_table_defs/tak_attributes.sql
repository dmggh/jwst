CREATE TABLE target_keyword ( 
        tak_program_id                  char(3)         NOT NULL,
        tak_obset_id                    char(2)         NOT NULL,
        tak_obsnum                      char(3)         NOT NULL,
        tak_broad_category              varchar(80)     NOT NULL,
        tak_keyword_text                varchar(68)     NOT NULL )
go
