#! /bin/csh

# This version of affected_datasets is optimized to make use of information
# contained in the context-to-context differences,  i.e. to only process 
# instruments and types with differences,  and to only process exposures
# which occured after USEAFTER dates in the differences.

affected_datasets $* --diffs-only --datasets-since=auto

