#! /usr/bin/env pysh

usage("<username> <password>", 2, 2)

username = sys.argv[1]
password = sys.argv[2]

import django
django.setup()
from django.contrib.auth.models import User

u = User.objects.get(username=username)
u.set_password(password)
u.save()

