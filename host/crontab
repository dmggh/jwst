# use /bin/tcsh to run commands, no matter what /etc/passwd says
SHELL=/bin/tcsh

# mail any output to jmiller
MAILTO=jmiller@stsci.edu

# run five minutes after 3 am, every day
5 3 * * *       source $HOME/cronenv.csh;      nightly.crds.job 

# every 5 minutes, no e-mail
*/5 * * * *     source $HOME/cronenv.csh;      monitor_reprocessing >& ${CRDS_CRON}/monitor_reprocessing.log

# every 10 minutes, no e-mail
*/10 * * * *  source $HOME/cronenv.csh;      sync_ops_to_grp >& ${CRDS_CRON}/sync_ops_to_grp.log

# once a week, Friday night at midnight + 4 minutes, check cache sha1sums
# don't do this on CRDS VM since it loads down the VM file buffer cache
# 04 0 * * fri  source $HOME/cronenv.csh;      sync_ops_to_grp --check-sha1sum  >& ${CRDS_CRON}/sync_ops_to_grp.sha1sum.log

# run at 2:15pm on the first of every month
#   15 14 1 * *     $HOME/bin/monthly
# run at 10 pm on weekdays, annoy Joe
#   0 22 * * 1-5   mail -s "It's 10pm" joe%Joe,%%Where are your kids?%
#   23 0-23/2 * * * echo "run 23 minutes after midn, 2am, 4am ..., everyday"
#   5 4 * * sun     echo "run at 5 after 4 every sunday"

