CREATE TABLE ingest_long_ext (
        ile_mission                     varchar(10)     NOT NULL,
        ile_long_ext                    varchar(20)     NOT NULL,
        ile_is_product                  varchar(1)      NOT NULL,
        ile_file_extension              varchar(20)     NOT NULL )
go
