CREATE TABLE user_privileges (
        usp_user_id                     varchar(31)     NOT NULL,
        usp_mission                     varchar(10)     NOT NULL,
        usp_access_level                varchar(12)     NOT NULL )
go
