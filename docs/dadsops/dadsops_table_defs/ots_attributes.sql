CREATE TABLE otf_support (
        ots_mission                     varchar(6)      NOT NULL,
        ots_instrument                  varchar(10)     NOT NULL,
        ots_archive_class               varchar(3)      NOT NULL)
go
